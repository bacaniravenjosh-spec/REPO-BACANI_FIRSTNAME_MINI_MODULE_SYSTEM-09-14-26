#Main Menu / PROGRAM ENTRY POINT 
# CODE PROPER
import sys
import math
import random 
import functools  

#FORCED OUTPUT FLUSHING
print =functools.partial(print, flush=True)

#HELPER FUNCTIONS 
def get_float(prompt):
    while True:
          try:
            print(prompt, end=" ", flush=True)
            sys.stdout.flush()
            return float(import())
        except ValueError"
            print(">>Invalid input: Please type a number(e.g., 3, 3.5). ")
        except EOFError:
            print("\n >> No input received. Exiting program. ")
            raise SystemExit
def safe_input(prompt):
               try:
                   print(prompt, end= " ", flush=True)
                   sys.stdout.flush()
                   return input()
               exceptEOFError:
                   print("\n >> No input received. Exiting program. ")
                   raise SystemExit
def pause():
        safe_input("\n press ENTER to return to the menu...")
def print_header(title)
        print("\n" + "=" * 70 )
        print(f" {title} ")
        print("=" * 70)

#SECTION 1: TRIGONOMETRIC (CIRCULAR) FUNCTIONS
def trig_menu():
    print_header("TRIGONOMETRIC (CIRCULAR) FUNCTIONS")
    print("""
    1. Show the value of pi
    2. convert degrees -> radians
    3. convert radians -> degrees
    4. compute sin, cos, atan of an angle (you enter degrees)
    5. compute asin, acos, atan, of a value (-1 to 1 for asin/acos)
    0. back to Main Menu 
""")
    choice = safe_input("Enter your choice: ").strip()

    if choice =="1":
        print(f"nmath.pi = {math.pi}")
        
    elif choice =="2":
        deg = get_float("enter an angle in degrees: ")
        rad = math.radians(deg)
        print(f"\n{deg} degrees = {rad} radians")

    elif choice =="3":
        rad = get_float("Enter an angle in radians: ")
        deg = math.degrees(rad)
        print(f"\n{rad} radians = {deg} degrees")

    elif choice == "4":
        deg = get_float("Enter an angle in degrees: ")
        rad = math.radians(deg)    #sin/cos/tan except radians !
        print(f"\nAngle = {deg} degrees ({rad} radians)")
        print(f"   sin({deg}) = {math.sin(rad)}")
        print(f"   cos({deg}) = {math.cos(rad)}")
        print(f"   tan({deg}) = {math.tan(rad)}")

    elif choice == "5":
        val = get_float("Enter a value")
        print()
        try:
            print(f"  asin({val}) = {math.asin(val)} radians "
                  f"({math.degrees(math.asin(val))} degrees)")
        except ValueError:
            print("  asin(x) needs -1 <= x <= 1. Skipped.")
        try:
            print(f"  acos({val}) = {math.acos(val)} radians "
                  f"({math.degrees(math.acos(val))} degrees)")
        except ValueError:
            print("  acos(x) needs -1 <= x <= 1. Skipped.")
        print(f"  atan({val}) = {math.atan(val)} radians "
              f" ({math.degrees(math.atan(val))} degrees)")
              
        elif choice == "0":
            return 
        else:
            print("\nInvalid choice.")

        pause()
#SECTION 2: HYPERBOLIC FUNCTIONS
def hyperbolic_menu():
    print_header("HYPERBOLIC FUNCTIONS")
    print("""
1.  Compute sinh, cosh, tanh of a value
    2. Compute asinh, acosh, atanh of a value
    0. Back to Main Menu 
""")
    choice = safe_input("Enter your choice: ").strip()

    if choice == "1":
        x = get_float("Enter a value x: ")
        print(f"\n  sinh({x}) = {math.sinh(x)}")
        print(f"    cosh({x}) = {math.cosh(x)}")
        print(f"    tanh({x}) = {math.tanh(x)}")

    elif choice == "2":
        x = get_float("Enter a value x: ")
        print()
        print(f"  asinh({x}) = {math.asinh(x)}")
    try:
        print(f"  acosh({x}) = {math.acosh(x)}")
    except ValueError:
        print(" acosh(x) needs x >= 1. Skipped.")
    try:
        print(f" atanh({x}) = {math.atanh(x)}")
    except ValueError:
        print("  atanh(x) needs -1 < x < 1. Skipped.")

    elif choice == "0":
        return
    else:
        print("\nInvalid choice.")

    pause()

#SECTION 3: EXPONENTIATION AND LOGARITHMS
                



def main():
    print("=" * 70)
    print("WELCOME TO THE MATH & RANDOM MODULE EXPLORER")
    print("A learning tool for Python's 'math' and 'random' modules")

    while true:
        print("""
MAIN MENU 
    1. Trigonometric (circular) functions    - sin, cos, tan, pi, radians...
    2. Hyperbolic functions                  - sinh, cosh, tann, asinh...
    3. Exponential & logerithmatic functions - e, exp, log, log10, log2, pow
    4. General purpose math functions        - ceil, floor, trune, factorial, hypot
    5. Random module                         - seed, randrange, randint, choice, sample
    0. Exit the program
""")
       choice = safe_input("Enter your choice (0-5): ").strip()

       if choice =="1":
          trig_menu()
       elif choice =="2":
          hyperbolic menu()
       elif choice =="3":
          exponent_menu()
       elif choice =="4":
          general_menu()
       elif choice =="5":
          random_menu()
       elif choice=="0":
          print("\nThank you for exploring the math and random modules. "
          "Goodbye!")
                    break
       else:
        print("\nInvalid choice. Please enter a number from 0 to 5.")

#standard Idiom: "only run main()
if_name_=="_main_":
    try
